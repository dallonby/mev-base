# MEV Logging Configuration
# Set the log level for the MEV system
# Options: trace, debug, info, warn, error
# Default: info
MEV_LOG=info

# Alternative: Use standard Rust logging (MEV_LOG takes precedence)
# RUST_LOG=info

# Examples of advanced filtering:
# MEV_LOG=debug                                    # Debug level for everything
# MEV_LOG=info,mevbase::mev_task_worker=debug      # Info globally, debug for mev_task_worker
# MEV_LOG=warn,mevbase=debug,revm=trace            # Warn globally, debug for mevbase, trace for revm
# MEV_LOG=error                                    # Only show errors